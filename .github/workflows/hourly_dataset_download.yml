name: Hourly velib availability download python script

on:
  pull_request:
    types: [opened, edited]
    branches:
      - main
      - dev
  push:
    branches:
      - fix/auto-download
  schedule:
    - cron: "0 * * * *"  # Run every hour

jobs:
  download-velib-data:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Install Poetry
        run: |
          curl -sSL https://install.python-poetry.org | python3 -

      - name: Setup Python & Install dependencies
        uses: actions/setup-python@v4
        with:
          python-version: '3.11.8'
          cache: 'poetry'
      - run: poetry install

      - name: Download Velib Availability Data
        env:
          BEARER_TOKEN: ${{ secrets.BEARER_TOKEN }}
        run:
          poetry run python src/velib_prediction/pipelines/download_data/download_data_run.py
    
      - name: Commit and push
        env:
          github_token: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add -A
          git commit -m "Data updated" -a
          git push
